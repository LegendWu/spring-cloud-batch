<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.clould.batch.mapper.BatchJobConfigMapper">

	<select id="selectConfigByCode" parameterType="map" resultType="com.spring.clould.batch.entity.BatchJobConfig">
		SELECT * FROM BATCH_JOB_CONFIG WHERE CONF_CODE=#{confCode}
	</select>
	
</mapper>